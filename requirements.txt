# ---- Core API
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
python-multipart==0.0.9
requests==2.31.0

# ---- Imaging
pillow==10.3.0
numpy==1.26.4
opencv-python-headless==4.9.0.80

# ---- YOLO (Ultralytics)
ultralytics==8.2.0

# ---- Torch CPU ONLY (avoid CUDA downloads)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2
torchvision==0.17.2

# ---- Your other deps (keep only if used)
rembg==2.0.50
google-genai==0.5.0
replicate==0.25.2

